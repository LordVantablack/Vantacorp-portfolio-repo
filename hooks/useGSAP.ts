// Re-export useGSAP from @gsap/react.
// Using this project-local path keeps imports consistent and makes it easy to
// add shared setup (e.g. auto-registering plugins) in one place if needed later.
export { useGSAP } from '@gsap/react';
